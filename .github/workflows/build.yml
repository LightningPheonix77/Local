name : Greet on Push

on : [push]

jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3  # Using an action
            - run: npm install           # Running a command
            - run: echo "Hello World"
              